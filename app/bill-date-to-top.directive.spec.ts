import { BillDateToTopDirective } from './bill-date-to-top.directive';

describe('BillDateToTopDirective', () => {
  it('should create an instance', () => {
    const directive = new BillDateToTopDirective();
    expect(directive).toBeTruthy();
  });
});
