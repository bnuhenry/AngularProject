import { DateToTopDirective } from './date-to-top.directive';

describe('DateToTopDirective', () => {
  it('should create an instance', () => {
    const directive = new DateToTopDirective();
    expect(directive).toBeTruthy();
  });
});
